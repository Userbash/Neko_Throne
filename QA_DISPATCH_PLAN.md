# Инструкция для Оркестратора: Масштабное улучшение тестирования и GUI (Neko_Throne)

## Контекст (Сбор данных завершен)
Сборка и базовые тесты в `qt-dev-box` прошли успешно. Ошибка компилятора (`isSystemdResolvedDefaultResolver`) была исправлена. `FullArtifactCheck` прошел успешно (10 passed, 0 failed). 
Статический анализ `clang-tidy` показал ошибки конфигурации только в `3rdparty/simple-protobuf` (ключ `AnalyzeTemporaryDtors` больше не поддерживается в новых версиях clang-tidy), исходный код `src/` чист от критических предупреждений.

Существующие тесты (`ArtifactTest.cpp`) слишком поверхностны:
- Они проверяют только наличие бинарников (`testBinariesExist`), запуск процессов (`testCoreExecution`, `testOffscreenStartup`) и базовую стабильность пустых потоков (`testThreadSafetySimulation`).
- Отсутствует реальное E2E тестирование взаимодействия пользователя с графическим интерфейсом.

---

## 🤖 CORE: ORCHESTRATOR_DISPATCH (Фаза 2: Глубокое E2E и UI/UX улучшение)

Оркестратору необходимо запустить следующий пул задач в режиме **MULTITHREADED**. 
`DISPATCH_MODE: FULL_AUTO`

### [Task Group 1: UI & E2E Testing Framework (Frontend Dev Agent + Tester Agent)]
**Описание:** Текущий подход "запуск и проверка кода возврата" недостаточен. Необходимо внедрить полноценное тестирование UI-компонентов.
1.  **Создание UI-Тестов:** Написать модульные тесты с использованием `QTest::mouseClick()`, `QTest::keyClick()` для основных экранов (например, `MainWindow`, `ProfileEditor`).
2.  **Мокирование Ядра:** Создать mock-интерфейс для подсистемы C++->Go (например, фейковый gRPC/RPC ответ от `NekoCore`), чтобы тестировать GUI без поднятия реального соединения.
3.  **Визуальные Регрессии:** Внедрить базовый скриншот-тестинг (сделать снимок экрана с помощью `QPixmap::grabWindow` при `offscreen` запуске и сравнить с эталоном).

### [Task 2: Улучшение `ArtifactTest.cpp` (C++ Fixer Agent)]
**Описание:** Усилить текущие "пустые" тесты.
- Заполнить `testConfigValidation` реальным созданием объекта конфигурации и проверкой валидации JSON/YAML.
- Усилить `testThreadSafetySimulation` реальными вызовами безопасных слотов (например, `SafeUIFunction`).
- Очистить/обновить `.clang-tidy` в корне проекта (убрать `AnalyzeTemporaryDtors: false`, так как это вызывает ошибку в LLVM 18+).

### [Task 3: Профилирование и Устранение Утечек GUI (Reviewer Agent)]
**Описание:** Глубокий анализ кода на предмет утечек памяти в Qt.
- Использовать инструмент ASAN (уже включен в CI) для прогона `Neko_Throne` в headless режиме и поиска утечек QObject, которые создаются без parent'а.
- Проанализировать `src/ui/models/` на предмет правильного использования `beginInsertRows`/`endInsertRows`, чтобы избежать крашей при обновлении UI.

### [Task 4: Архитектурный рефакторинг вызовов (Architect Agent)]
**Описание:** Изолировать платформозависимые вызовы.
- Провести рефакторинг `src/sys/` чтобы все `#ifdef Q_OS_WIN` и `Q_OS_LINUX` были скрыты за единым интерфейсом абстрактной фабрики (например, `IPlatformInterface`), чтобы UI-код вообще не знал о платформе.

**PRIORITY: CRITICAL**
**EXECUTE_IMMEDIATELY**
